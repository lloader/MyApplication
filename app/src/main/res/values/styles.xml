<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@drawable/search_field_white_rounded</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:editTextColor">@color/black</item>
        <item name="searchIcon">@drawable/ic_search</item>
        <item name="android:hint">@string/search_view_hint</item>
        <item name="closeIcon">@drawable/ic_lose</item>
        <item name="android:queryHint">@string/search_view_hint</item>
        <item name="android:textColorHint">#8b000000</item>
        <item name="submitBackground">@android:color/transparent</item>
        <item name="queryBackground">@android:color/transparent</item>
    </style>

    <style name="FilmDescribeTextStyle">
        <item name="android:textColor">#8b000000</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
